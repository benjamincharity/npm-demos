/*
 *import dayTemplate from './day.html';
 *console.log('dayTemplate: ', dayTemplate);
 */

export function CalendarConfig(
    bcCalendarConfigProvider
) {
    'ngInject';

    /*
     *const template = '<span ng-if="day.date">TODAY: {{day.date | date:"d"}}</span>';
     *bcCalendarConfigProvider.setDayTemplate(template);
     */

    /*
     *bcCalendarConfigProvider.showHeader = false;
     */

}

